#include <stdio.h>
#include <math.h>

int main(){
	int number;
	scanf("%d" ,&number);
	if(number > 0 ){
		printf("%d la so duong",number);
	}else{
		printf("%d la so am" ,number);
	}
	
	return 0;
}
